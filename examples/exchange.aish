#a1 shared scan of payments module, 2 agents appending
@ agent:scout
1| . payments/charge.go:200-260 = retry wrapper
2| ! retry wraps non-idem POST /v1/charge -> double charge risk
  . no idem key sent
  . backoff 3 attempts, 200ms
3| ? does upstream dedupe on its side
* live money path, do not "fix" without owner sign-off

@ agent:verifier
+ confirmed [2] by replay, 2 charges landed in sandbox
- upstream dedupe [3] = false, checked vendor docs
4| . vendor supports header "Idempotency-Key" on this endpoint
> send stable idem key per charge attempt [4] ^ owner sign-off [*]
~ existing duplicates in production since retry shipped %60
 > query ledger for same-amount pairs <60s apart
